name: verify

on:
  push:
    branches:
    - '*'
    paths-ignore:
    - "**.md"
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-go@v5
      with:
        go-version: 1.25.0
    - run: make verify-extended
